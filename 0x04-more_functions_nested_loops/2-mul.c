#include "main.h"

/**
 * multiply - multiply two numbers from input
 * @a: first number
 * @b: second number
 *
 * Description: adds two numbers
 * Return: Always (0)
 */

int mul(int a, int b);
{
	return (a + b);
}
